/*
File: ImageLoader.h
Author: Abramau Mikalai
Description: Contains function prototypes to load images. Orientation
to opengl
*/

#ifndef _H_IMAGE_LOADER
#define _H_IMAGE_LOADER

#include "image_file.h"

namespace ImageLoader
{
/*	static int constructorCount = 0;
	static int destructorCount;

//	extern void LoadDDS(const char* filename, ImageFile* ImageFile);
	extern ImageFile *Load(const char* filename);
	extern void Free(ImageFile* image);
	extern ImageFile *ConvertHeightMapToNormalMap(const ImageFile* surface, float scale = 1.0);
	extern void BuildMipMaps(ImageFile* image);
	extern void BuildMipMapsForNormalMap(ImageFile* normalMap);
	extern void Save(Surface* surface, int numComponents, const char* filename);*/
}

#endif	//	_H_IMAGE_LOADER
