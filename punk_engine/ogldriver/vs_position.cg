
float4x4 worldViewProj;

struct vsInPosition
{
	float3 position : POSITION;
};

struct vsOutPosition
{
	float4 position : POSITION;
};

vsOutPosition vsPosition(vsInPosition vsIn)
{
	vsOutPosition vsOut;
	vsOut.position = mul(worldViewProj, float4(vsIn.position, 1.0));
	return vsOut;
}