#ifndef _LIGHT_MANAGER
#define _LIGHT_MANAGER

/*
#include <windows.h>
#include <gl\gl.h>
#include <gl\glext.h>
#include "../PunkEngine/PunkEngine.h"
#include "Light.h"

#define PUNK_MAX_LIGHTS 8

class TLightManager : public ILightManager
{
public:
	TLightManager();
	TLightManager(const TLightManager& lightManager);
	TLightManager& operator = (const TLightManager& lightManager);

	virtual ~TLightManager();
	virtual void EnableLighting();
	virtual void DisableLighting();

	virtual ILight* GetLight(int id);

	virtual void SetAmbient(float r, float g, float b, float a);
private:
	TLight m_light[PUNK_MAX_LIGHTS];
	float m_ambient[4];
};
*/
#endif
